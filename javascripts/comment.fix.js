//this fixes the submit comment button so it matches foundation style
//method found in http://wordpress.stackexchange.com/a/60197/25300
jQuery(document).ready(function($) { //noconflict wrapper
    $('input#submit').addClass('button');
});//end noconflict
